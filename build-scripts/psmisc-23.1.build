#! /bin/bash
patch -Np1 -i ../psmisc-23.1-musl-ptregs.patch &&
./configure --prefix=/usr && 
read -p "Compile? " && make -j2 && 
read -p "Install? " && porg -lD "make install -j1" &&
porg -lD+ "mv -v /usr/bin/fuser   /bin" &&
porg -lD+ "mv -v /usr/bin/killall /bin"
