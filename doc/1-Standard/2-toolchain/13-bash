# Toolchain: bash
# Build and install as mlfs

# Configure source
./configure --prefix=/tools \
            --without-bash-malloc 

# Build and install to toolchain
make && make install

# Create a link for sh
ln -sv bash /tools/bin/sh

