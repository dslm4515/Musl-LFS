# Toolchain: M4
# Build and install as mlfs

# Configure source
./configure --prefix=/tools \
            --build=${MLFS_HOST} \
            --host=${MLFS_TARGET}

# Build and install to toolchain
make && make install
