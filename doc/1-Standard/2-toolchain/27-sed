# Toolchain: Sed
# Build and install as mlfs

# Use kernel header instead
patch -Np1 -i ../patches/sed-mlfs/use-linux-limits-header.patch

# Configure source
./configure --prefix=/tools 

# Build and install to toolchain
make && make install
