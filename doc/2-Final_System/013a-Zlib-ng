# Final System: Zlib-ng 
# This section is done in Chroot environment
# Alternative to Zlib

# Configure source
./configure  --prefix=/usr \
             --libdir=/usr/lib \
             --zlib-compat \
             --sharedlibdir=/usr/lib 

# Build and Install
make 
make install

# Add missing combatibility link
ln -sv libz.so.1.3.1.zlib-ng /usr/lib/libz.so.1.3.1
