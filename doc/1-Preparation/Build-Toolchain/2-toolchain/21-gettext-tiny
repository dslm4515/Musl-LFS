# Toolchain: Gettext-tiny
# Build and install as mlfs

# Use the limits.h header in include/linux
patch -Np1 -i ../patches/gettext-tiny-mlfs/use-linux-limits-header.patch 

# Build and install to toolchain
make ${MJ} LIBINTL=MUSL prefix=/tools      
cp -v msgfmt msgmerge xgettext /tools/bin 
