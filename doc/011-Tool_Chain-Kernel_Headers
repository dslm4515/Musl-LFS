# Tool Chain: Kernel Headers
# Build and install as mlfs

# Clean source
make mrproper

# Extract headers
ARCH=${MLFS_CPU} make INSTALL_HDR_PATH=dest headers_install

find dest/include \( -name .install -o -name ..install.cmd \) -delete

# Install
cp -rv dest/include/* /tools/include
