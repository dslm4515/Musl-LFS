# Final System: Attr 
# This section is done in Chroot environment

./configure --prefix=/usr     \
            --bindir=/usr/bin     \
            --disable-static  \
            --sysconfdir=/etc \
            --docdir=/usr/share/doc/attr-2.5.1

make && make install
