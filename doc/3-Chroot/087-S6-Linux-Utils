# Final System: S6-Linux-Utils 
# This section is done in Chroot environment

# Configure
./configure --disable-allstatic \
            --prefix=/usr       \
            --with=dynlib=/lib

# Compile
make

# Install
make install
