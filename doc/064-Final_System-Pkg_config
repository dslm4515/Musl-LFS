# Final System: Pkg-Config
# Source: https://pkg-config.freedesktop.org/releases/pkg-config-0.29.2.tar.gz
# This section is done in Chroot environment

# Configure source
./configure --prefix=/usr              \
            --with-internal-glib       \
            --disable-host-tool        \
            --docdir=/usr/share/doc/pkg-config-0.29.2

# Build and Install
make && make install
