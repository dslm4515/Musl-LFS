# Final System: Grep 3.5
# Source: http://ftp.gnu.org/gnu/grep/grep-3.5.tar.xz
# This section is done in Chroot environment

./configure --prefix=/usr --bindir=/bin \
            --sysconfdir=/etc  && make && make install
