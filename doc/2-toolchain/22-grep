# Toolchain: Grep
# Build and install as mlfs

# Use kernel header instead for limits
patch -Np1 -i ../patches/grep-mlfs/use-linux-limits-header.patch 

# Configure source
./configure --prefix=/tools 

# Build and install to toolchain
make && make install
